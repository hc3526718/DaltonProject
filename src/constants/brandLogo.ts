/**
 * Legacy PNG alias. Prefer `BrandLogo` / `brandAssets`.
 */
export { DALTON_LOGO_FINAL_PNG as BRAND_LOGO_PNG } from './brandAssets';

/** @deprecated Use `BrandLogo` / `DALTON_LOGO_FINAL_IMG` from `./brandAssets` (same file as legacy `DALTON_LOGO_FINAL_PNG`). */
export { DALTON_LOGO_FINAL_PNG as BRAND_LOGO } from './brandAssets';
